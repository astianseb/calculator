steps:
    - name: 'gcr.io/cloud-builders/docker'
      args: ['build', '-t', 'eu.gcr.io/$PROJECT_ID/${SERVICE_NAME}:$COMMIT_SHA', '.']
    - name: 'gcr.io/cloud-builders/docker'
      args: ['push', 'gcr.io/$PROJECT_ID/${SERVICE_NAME}:$COMMIT_SHA']
    - name: 'gcr.io/cloud-builders/gcloud'
      args:
        - 'run'
        - 'deploy'
        - '${SERVICE_NAME}'
        - '--image'
        - 'gcr.io/$PROJECT_ID/${SERVICE_NAME}:$COMMIT_SHA'
        - '--region'
        - '${REGION}'
        - '--platform'
        - 'managed'
images:
    - 'gcr.io/$PROJECT_ID/${SERVICE_NAME}:$COMMIT_SHA'
substitutions:
    REGION: europe-west4
    SERVICE_NAME: sg-calculator-test